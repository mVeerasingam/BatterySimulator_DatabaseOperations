server:
  port: 8082

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: guest
    password: guest
  h2:
    console:
      enabled: true
      path: /h2-console